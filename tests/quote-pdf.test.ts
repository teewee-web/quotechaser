import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{calculateQuoteTotals,compatibleItems,defaultExpiry,formatQuoteNumber,safePdfFilename}from"../lib/quote-pdf";
describe("quotation calculations",()=>{const items=[{description:"Labour",quantity:2,unitPricePence:10000},{description:"Materials",quantity:1,unitPricePence:5000}];it("calculates line totals and subtotal",()=>expect(calculateQuoteTotals(items).subtotalPence).toBe(25000));it("applies a fixed discount",()=>expect(calculateQuoteTotals(items,"fixed",25).finalTotalPence).toBe(22500));it("applies percentage discount and VAT",()=>expect(calculateQuoteTotals(items,"percentage",10,true,20)).toEqual({subtotalPence:25000,discountPence:2500,vatPence:4500,finalTotalPence:27000}));it("supports old quotes without items",()=>expect(compatibleItems([],12345,"Decorating")).toEqual([{description:"Decorating",quantity:1,unitPricePence:12345,sortOrder:0}]))});
describe("quotation identity",()=>{it("formats per-year sequence numbers",()=>expect(formatQuoteNumber(2026,1)).toBe("QC-2026-0001"));it("creates safe filenames",()=>expect(safePdfFilename("QC-2026-0001","John O'Neill / Smith")).toBe("Quote-QC-2026-0001-John-O-Neill-Smith.pdf"));it("defaults expiry from quote date",()=>expect(defaultExpiry("2026-07-14",30)).toBe("2026-08-13"))});
describe("user data isolation",()=>{it("enables RLS and ownership checks for quote items and logos",()=>{const sql=readFileSync("supabase/migrations/202607140002_pdf_quotes.sql","utf8");const hardening=readFileSync("supabase/migrations/202607170001_launch_hardening.sql","utf8");expect(sql).toContain("alter table public.quote_items enable row level security");expect(sql).toContain("user_id=(select auth.uid())");expect(sql).toContain("(storage.foldername(name))[1]=(select auth.uid())::text");expect(hardening).toContain("with check (bucket_id = 'business-logos'")})});
