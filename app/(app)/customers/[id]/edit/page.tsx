import { notFound } from "next/navigation";import { createClient } from "@/lib/supabase/server";import { PageHeader } from "@/components/nav";import { CustomerForm } from "@/components/customer-form";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params,s=await createClient(),{data}=await s.from("customers").select("*").eq("id",id).single();if(!data)notFound();return <><PageHeader title="Edit customer"/><CustomerForm customer={data}/></>}
