import{createClient}from"@/lib/supabase/server";import{PageHeader}from"@/components/nav";import{QuoteForm}from"@/components/quote-form";
export default async function Page(){const s=await createClient();const[{data=[]},{data:settings}]=await Promise.all([s.from("customers").select("*").order("name"),s.from("user_settings").select("vat_registered,default_vat_rate,default_payment_terms,default_terms_and_conditions,default_quote_validity_days").maybeSingle()]);return <><PageHeader title="Create customer quote" description="Add the customer and build their professional quotation in one quick flow."/><QuoteForm customers={data||[]} defaults={settings||{}}/></>}
